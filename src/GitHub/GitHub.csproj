<Project Sdk="Microsoft.NET.Sdk">

	<PropertyGroup>
		
		<TargetFramework>netstandard2.1</TargetFramework>
		<Nullable>enable</Nullable>
		<RootNamespace>Zhis.Utilities</RootNamespace>
		<AssemblyName>$(RootNamespace).GitHub</AssemblyName>
		<GeneratePackageOnBuild>True</GeneratePackageOnBuild>
		<Title>Zhi's Utilities of GitHub</Title>
		<VersionPrefix>1</VersionPrefix>
		<Version>$(VersionPrefix).3.5</Version>
		<Authors>zhiliang</Authors>
		<Company />
		<Description>Wraps some of the frequently used features from the GitHub SDK into a smoother toolset.</Description>
		<Copyright>Copyright 2022-2121 Zhi Liang</Copyright>
		<PackageProjectUrl>https://github.com/zhiliangpt/Zhis.Utilities</PackageProjectUrl>
		<RepositoryUrl>https://github.com/zhiliangpt/Zhis.Utilities</RepositoryUrl>
		<PackageTags>.net;standard;aws;library;utilities;</PackageTags>
		<PackageReleaseNotes>
			Added isIncluded parameter in RequestCommitsSince.
		</PackageReleaseNotes>
		<PackageReadmeFile>readme.md</PackageReadmeFile>
		<PackageLicenseExpression>MIT</PackageLicenseExpression>
		<GenerateDocumentationFile>True</GenerateDocumentationFile>

	</PropertyGroup>

	<ItemGroup>
	  <PackageReference Include="Markdig" Version="0.37.0" />
	  <PackageReference Include="Octokit" Version="11.0.1" />
	</ItemGroup>

	<ItemGroup>
	  <None Update="readme.md">
	    <Pack>True</Pack>
	    <PackagePath>\</PackagePath>
	  </None>
	</ItemGroup>

</Project>
